# Docs assets

- `demo.gif` — illustrative README / site hero (from `specs/readme-demo.termgif.json`).
- Regenerate: `python3 ~/.cursor/skills/shipmates-termgif/termgif.py --spec specs/readme-demo.termgif.json --out demo.gif` (or the vendored path your environment uses).

Frames are **illustrative** until the golden-suite ledger ships real numbers.
